# @package task

# Task: Predict subgoal_label (binary classification)
name: subgoal_label
prediction_type: binary_classification

# Feature indices in the 21-feature format:
# [x(0), y(1), robot_type(2-6), has_goal(7-8), walls(9-13),
#  helper1_goal_pos(14), helper2_goal_pos(15), helper3_goal_pos(16),
#  helper_aggregate_goal_pos(17), target_goal_pos(18),
#  chosen_helper(19), subgoal_label(20)]

target_index: 20  # subgoal_label (index 20 in 21-feature format)

# Only use the 14 base features (no goal position features)
include_goal_features: []
