# @package task

# Task: Predict helper_pos_aggregate_goal_pos (binary classification)
name: helper_aggregate
prediction_type: binary_classification

# Feature indices in the 21-feature format:
# [x(0), y(1), robot_type(2-6), has_goal(7-8), walls(9-13),
#  helper1_goal_pos(14), helper2_goal_pos(15), helper3_goal_pos(16),
#  helper_aggregate_goal_pos(17), target_pos_goal_pos(18),
#  chosen_helper(19), subgoal_label(20)]

target_index: 17  # helper_pos_aggregate_goal_pos (index 17 in 21-feature format)

# Use 14 base features + subgoal_label
include_goal_features: [20]  # subgoal_label
